@model IEnumerable<MvcBanHang.Models.hangsanxuat>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<script type="text/javascript">
    $(function () {
        $('.RemoveLink').click(function () {
            var x = $(this).attr("data-id");
            if (confirm("Đối tượng sẽ bị xóa.Bạn có muốn không ?")) {
                $.ajax({
                    type: "POST",
                    url: '@Url.Action("Delete", "Manufacture")',
                    data: { id: x }
                });
                $("#row-" + x).remove();
            }
        })
    })   
</script>
<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table>
    <tr>
        <th>
            Tên hãng sản xuất
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr id="row-@item.mahsx">
            <td>
                @Html.DisplayFor(modelItem => item.tenhsx)
            </td>
            <td>
                @Html.ActionLink("Sửa", "Edit", new { id = item.mahsx }) |    
            <a href="#" class="RemoveLink" data-id="@item.mahsx">Xóa</a>
            </td>
        </tr>
    }
</table>

